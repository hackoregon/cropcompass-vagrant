---
##
# Change box settings to fit project
#  - Multiple boxes can be defined, but will need different names.
- name: 'ubuntu'
  box: 'ubuntu/trusty64'
  memory: 1024
  provision_shell:
    - 'scripts/installs.bash'
  post_up_message: 'Crop Compass server provisioning completed'
